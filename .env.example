# --- Docker Engine access ---------------------------------------------------
# Production (compose): the socket proxy on the internal network.
DOCKER_HOST=tcp://socket-proxy:2375
# Local dev against the homelab over an SSH tunnel:
#   ssh -N -L 127.0.0.1:12375:/var/run/docker.sock homelab@192.168.1.70
# DOCKER_HOST=tcp://127.0.0.1:12375

# --- Auth -------------------------------------------------------------------
# Generate with: npm run hash-password -- 'your-password'
ADMIN_PASSWORD_HASH=
# Any long random string; sessions are invalidated when it changes.
SESSION_SECRET=

# --- Dashboard --------------------------------------------------------------
# LAN IP used to build "open app" URLs for containers with published ports.
PUBLIC_HOST=192.168.1.70
# Where user config (widgets, groups, icons) is persisted.
DATA_DIR=./data
# Link target for the "Stacks (Dockge)" nav item.
DOCKGE_URL=http://192.168.1.70:5001
